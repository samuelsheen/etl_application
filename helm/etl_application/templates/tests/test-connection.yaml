apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "etl_application.fullname" . }}-test-connection"
  labels:
    {{- include "etl_application.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "etl_application.fullname" . }}:{{ .Values.servicePort }}']
  restartPolicy: Never
